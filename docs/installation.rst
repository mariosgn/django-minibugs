============
Installation
============

At the command line::

    $ easy_install django-minibugs

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-minibugs
    $ pip install django-minibugs
